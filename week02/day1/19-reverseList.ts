'use strict';
let numList: number[] = [3, 4, 5, 6, 7];
numList = numList.reverse();
console.log('with revers method: ' + numList);
// with a for loop:
/*console.log('with a for loop');
for (let index: number = numList.length-1; index >= 0; index--){
    console.log(numList[index]);
}*/