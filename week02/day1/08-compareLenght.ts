'use strict';
let firstList: number[] = [1, 2, 3];
let secondList: number[] = [4, 5];
if (secondList.length > firstList.length) {
    console.log('it has more elements yay');
} else {
    console.log('the first list has more elements.')
}