'use strict';
//for is a loop, it will repeat itself until the set value will check.
//the backward slash \ will enable the use of special characters in strings. 
for (let i: number = 1 ; i <= 100; i++) {
    console.log('I won\'t cheat on the exam!');
}